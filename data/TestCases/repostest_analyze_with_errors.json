{
    "functionName": "test_analyze_with_errors",
    "className": null,
    "fileName": "/bio-phys_&_MDBenchmark/mdbenchmark_&_tests_&_test_analyze.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "\"\"\"Test that we warn the user of errors in the output files. Also test that we\nshow a question mark instead of a float in the corresponding cell.\n    \"\"\"\nwith tmpdir.as_cwd():\n    result = cli_runner.invoke(cli, ['analyze', '--directory={}'.format(\n        data['analyze-files-w-errors'])])\n    bundle = dtr.discover(data['analyze-files-w-errors'])\n    df = DataFrameFromBundle(bundle)\n    df = df.replace(np.nan, '?')\n    test_output = PrintDataFrame(df, False) + '\\n'\n    assert result.exit_code == 0\n    assert result.output == test_output\n"
}