{
    "functionName": "test_tree_normal",
    "className": "TestConfigurationMethods",
    "fileName": "/XHotSniperX_&_Netconflib/tests_&_test_netconf.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "\"\"\"Tests the tree configurator.\n        \"\"\"\nfor num_nodes in range(1, self.MAX_NUM + 1, 8):\n    for degree in range(1, self.MAX_DEGREE + 1):\n        for root in range(0, num_nodes, 8):\n            with self.subTest(num_nodes=num_nodes, degree=degree, root=root):\n                self.logger.info(\n                    'Starting subtest with %d nodes, degree=%d and root=%d.',\n                    num_nodes, degree, root)\n                self.init_configurator(num_nodes)\n                self.ncl.configure_tree_topology(root, degree, False)\n                self.assertEqual(len(self.ncl.topology.nodes), num_nodes)\n                for node in self.ncl.topology.nodes:\n                    unique_gateways = len(set(node.get_all_gateways()))\n                    assert unique_gateways <= degree + 1, \"Node has more unique gateways than the tree's degree + parent!\"\n"
}