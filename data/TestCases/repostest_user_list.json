{
    "functionName": "test_user_list",
    "className": null,
    "fileName": "/moogar0880_&_PyTrakt/tests_&_test_users.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "sean = User('sean')\nassert all([isinstance(l, UserList) for l in sean.lists])\ndata = dict(name='Star Wars in machete order', description=\n    'Some descriptive text', privacy='public', display_numbers=True)\nl = UserList.create(creator=sean.username, **data)\nfor k, v in data.items():\n    assert getattr(l, k) == v\nl = UserList.get(data['name'], sean.username)\nl = sean.get_list(data['name'])\nfor k, v in data.items():\n    assert getattr(l, k) == v\ninstancetypes = Movie, TVShow, TVSeason, TVEpisode, Person\nassert all([isinstance(k, instancetypes) for k in l])\nl.add_items()\nfor k, v in data.items():\n    assert getattr(l, k) == v\nl.remove_items()\nfor k, v in data.items():\n    assert getattr(l, k) == v\nl.like()\nl.unlike()\nl.__iter__()\nl.delete_list()\n"
}