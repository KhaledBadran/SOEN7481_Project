{
    "functionName": "test_ctgan_numpy",
    "className": null,
    "fileName": "/DAI-Lab_&_CTGAN/tests_&_integration_&_test_ctgan.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "data = pd.DataFrame({'continuous': np.random.random(100), 'discrete': np.\n    random.choice(['a', 'b', 'c'], 100)})\ndiscrete_columns = [1]\nctgan = CTGANSynthesizer()\nctgan.fit(data.values, discrete_columns, epochs=1)\nsampled = ctgan.sample(100)\nassert sampled.shape == (100, 2)\nassert isinstance(sampled, np.ndarray)\nassert set(np.unique(sampled[:, (1)])) == {'a', 'b', 'c'}\n"
}