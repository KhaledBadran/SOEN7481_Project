{
    "functionName": "test_sos_phys2filter",
    "className": null,
    "fileName": "/PTB-PSt1_&_PyDynamic/test_&_test_second_order_systems.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "b, a = sos_phys2filter(S0, delta, f0)\nH = freqs(b, a, 2 * np.pi * fe5)[1]\nindmax = np.abs(H).argmax()\nassert np.round(np.abs(f0 - fe5[indmax])) <= 0.01 * f0\nassert np.abs(H[0]) == approx(S0)\nbmulti, amulti = sos_phys2filter(S0 * np.ones(K), delta * np.ones(K), f0 *\n    np.ones(K))\nassert len(bmulti[0]) == K\nassert amulti.shape == (K, 3)\n"
}