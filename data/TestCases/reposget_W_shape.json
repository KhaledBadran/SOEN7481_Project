{
    "functionName": "get_W_shape",
    "className": "LocallyConnected2DLayer",
    "fileName": "/Lasagne_&_Lasagne/lasagne_&_layers_&_local.py",
    "projectName": "repos",
    "Label": false,
    "isTest": false,
    "Body": "if any(s is None for s in self.input_shape[1:]):\n    raise ValueError(\n        'A LocallyConnected2DLayer requires a fixed input shape (except for the batch size). Got %r.'\n         % (self.input_shape,))\nnum_input_channels = self.input_shape[1]\noutput_shape = self.get_output_shape_for(self.input_shape)\nif self.channelwise:\n    if self.channelwise and self.num_filters != num_input_channels:\n        raise ValueError(\n            'num_filters and the number of input channels should match when channelwise is true, but got num_filters=%r and %d input channels'\n             % (self.num_filters, num_input_channels))\n    return (self.num_filters,) + self.filter_size + output_shape[-2:]\nelse:\n    return (self.num_filters, num_input_channels\n        ) + self.filter_size + output_shape[-2:]\n"
}