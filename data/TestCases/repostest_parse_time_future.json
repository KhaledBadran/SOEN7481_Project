{
    "functionName": "test_parse_time_future",
    "className": "TestParser",
    "fileName": "/akolar_&_ogn-lib/tests_&_test_parser.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "for i in range(5):\n    now = datetime.utcnow()\n    other = now + timedelta(minutes=i)\n    parsed = parser.Parser._parse_time(other.strftime('%H%M%S'))\n    delta = (parsed - now).total_seconds()\n    assert (i - 1) * 60 <= delta <= i * 60\n"
}