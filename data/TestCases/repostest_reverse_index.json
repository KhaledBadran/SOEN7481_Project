{
    "functionName": "test_reverse_index",
    "className": "TestLoopContext",
    "fileName": "/whiteclover_&_Choco/test_&_test_loop.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "length = len(self.iterable)\nexpected = tuple([(length - i - 1) for i in range(length)])\nactual = tuple(self.ctx.reverse_index for i in self.ctx)\nprint(expected, actual)\nassert expected == actual, 'The reverse_index is the number of iterations until the end'\n"
}