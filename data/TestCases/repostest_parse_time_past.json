{
    "functionName": "test_parse_time_past",
    "className": "TestParser",
    "fileName": "/akolar_&_ogn-lib/tests_&_test_parser.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "for i in range(24):\n    now = datetime.utcnow()\n    other = now - timedelta(hours=i)\n    parsed = parser.Parser._parse_time(other.strftime('%H%M%S'))\n    delta = (now - parsed).total_seconds()\n    assert 0 <= delta <= 86400\n"
}