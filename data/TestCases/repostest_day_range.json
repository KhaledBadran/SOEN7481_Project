{
    "functionName": "test_day_range",
    "className": null,
    "fileName": "/roniemartinez_&_DocCron/tests_&_test_range.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "cron = doccron.cron('* * 20-25 * * *')\nnext_datetime = datetime.now(tz=tzlocal()).replace(hour=0, minute=0, second\n    =0, microsecond=0)\nfor _ in range(25):\n    while next_datetime.day not in range(20, 26):\n        next_datetime += timedelta(days=1)\n    while next_datetime <= datetime.now(tz=tzlocal()):\n        next_datetime += timedelta(minutes=1)\n    next_schedule = next(cron)\n    assert isinstance(next_schedule, datetime)\n    assert next_schedule == next_datetime\n    next_datetime += timedelta(minutes=1)\n"
}