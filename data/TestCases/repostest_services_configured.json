{
    "functionName": "test_services_configured",
    "className": null,
    "fileName": "/EclecticIQ_&_OpenTAXII/tests_&_test_server.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "assert len(server.get_services()) == len(SERVICES)\nwith_paths = [s for s in server.get_services() if s.path]\nassert len(with_paths) == len(INTERNAL_SERVICES)\nassert all([p.address.startswith(DOMAIN) for p in with_paths])\n"
}