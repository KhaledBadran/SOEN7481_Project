{
    "functionName": "test_dasch_1d_gaussian",
    "className": null,
    "fileName": "/PyAbel_&_PyAbel/abel_&_tests_&_test_dasch_methods.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "def gauss(r, r0, sigma):\n    return np.exp(-(r - r0) ** 2 / sigma ** 2)\nrows, cols = n, n\nr2 = rows // 2\nc2 = cols // 2\nsigma = 20 * n / 100\nr = np.linspace(0, c2 - 1, c2)\norig = gauss(r, 0, sigma)\nfor method in dasch_transforms.keys():\n    orig_copy = orig.copy()\n    recon = dasch_transforms[method](orig)\n    ratio_1d = np.sqrt(np.pi) * sigma\n    np.testing.assert_allclose(orig_copy[20:], recon[20:] * ratio_1d, rtol=\n        0.0, atol=0.5)\n"
}