{
    "functionName": "process_buffer",
    "className": null,
    "fileName": "/dozymoe_&_PyCircularBuffer/tests_&_test_sms.py",
    "projectName": "repos",
    "Label": false,
    "isTest": false,
    "Body": "with buf:\n    match = regex.match(buf)\nif found:\n    assert match is not None\nelse:\n    assert match is None\n    return\nmatch_str = match.group(0)\nmatch_str = buf.read(len(match_str))\nmatch = regex.match(match_str)\nif found:\n    assert match is not None\n"
}