{
    "functionName": "test_value_to_count_matrix",
    "className": "TestMatrixAggregation",
    "fileName": "/NuttyLogic_&_BSBolt/tests_&_test_matrix_assembly.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "for value_values, count_values in zip(missing_and_low_count.meth_matrix,\n    missing_and_low_count_meth_values):\n    if np.isnan(sum(value_values)):\n        self.assertEqual(sum([np.isnan(sum(value_values)), np.isnan(sum(\n            count_values))]), 2)\n    else:\n        self.assertAlmostEqual(sum(value_values), sum(count_values))\n"
}