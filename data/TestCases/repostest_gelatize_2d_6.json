{
    "functionName": "test_gelatize_2d_6",
    "className": null,
    "fileName": "/ratnania_&_GeLaTo/gelato_&_tests_&_test_gelatize_2d.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "domain = Domain('Omega', dim=DIM)\nV = ScalarFunctionSpace('V', domain)\nu, v = elements_of(V, names='u,v')\nnx, ny = symbols('nx ny', integer=True)\npx, py = symbols('px py', integer=True)\ntx, ty = symbols('tx ty')\nexpected = nx * Mass(py, ty) * Stiffness(px, tx) / ny + I * Advection(px, tx\n    ) * Mass(py, ty) / ny + ny * Mass(px, tx) * Stiffness(py, ty\n    ) / nx + I * Advection(py, ty) * Mass(px, tx) / nx\nexpr = dot(grad(v), grad(u)) + dx(u) * v + dy(u) * v\nexpr = BilinearForm((u, v), integral(domain, expr))\nassert gelatize(expr) == expected\n"
}