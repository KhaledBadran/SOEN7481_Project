{
    "functionName": "test_nM_watson_smoke",
    "className": null,
    "fileName": "/LSSTDESC_&_CCL/pyccl_&_tests_&_test_hmfunc.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "nM = ccl.halos.MassFuncWatson13(COSMO, mdef)\nfor m in MS:\n    n = nM.get_mass_function(COSMO, m, 0.9)\n    assert np.all(np.isfinite(n))\n    assert np.shape(n) == np.shape(m)\nfor m in MS:\n    n = nM.get_mass_function(COSMO, m, 0.1)\n    assert np.all(np.isfinite(n))\n    assert np.shape(n) == np.shape(m)\npytest.mark.parametrize('mdef', [MFOF, M200m])"
}