{
    "functionName": "test_multiply",
    "className": null,
    "fileName": "/apetri_&_LensTools/lenstools_&_tests_&_test_ensemble.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "conv_ensemble_peaks = Ensemble.compute(file_list=map_list, callback_loader=\n    peaks_loader, pool=None, thresholds=thresholds_pk)\nconv_ensemble_both = Ensemble.concat([conv_ensemble, conv_ensemble_peaks],\n    axis=1)\nassert conv_ensemble_both.nobs == 4\nassert conv_ensemble_both.shape[0] == 4\nassert conv_ensemble_both.shape[1] == len(l_edges) + len(thresholds_pk) - 2\n"
}