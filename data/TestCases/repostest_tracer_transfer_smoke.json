{
    "functionName": "test_tracer_transfer_smoke",
    "className": null,
    "fileName": "/LSSTDESC_&_CCL/pyccl_&_tests_&_test_tracers.py",
    "projectName": "repos",
    "Label": false,
    "isTest": true,
    "Body": "tr, ntr = get_tracer(tracer_type)\nfor lk in [np.linspace(-3.0, 1.0, 10), [-2.0, 0.0], -1.0]:\n    for a in [np.linspace(0.5, 1.0, 8), [0.4, 1.0], 0.9]:\n        tf = tr.get_transfer(lk, a)\n        assert tf.shape[0] == ntr\n        if ntr > 0:\n            if np.ndim(a) == 0:\n                if np.ndim(lk) == 0:\n                    shap = ntr,\n                else:\n                    shap = ntr, len(lk)\n            elif np.ndim(lk) == 0:\n                shap = ntr, len(a)\n            else:\n                shap = ntr, len(lk), len(a)\n        else:\n            shap = 0,\n        assert tf.shape == shap\npytest.mark.parametrize('tracer_type', ['nc', 'wl', 'cl', 'not'])"
}