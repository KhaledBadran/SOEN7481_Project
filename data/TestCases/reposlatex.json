{
    "functionName": "latex",
    "className": "InclineLinear",
    "fileName": "/jakob-lass_&_MJOLNIR/MJOLNIR_&_Statistics_&_FittingFunction.py",
    "projectName": "repos",
    "Label": false,
    "isTest": false,
    "Body": "if not highlight is None:\n    if highlight == 0:\n        highlight = 1\n    elif highlight == 1:\n        highlight = 0\n    highlight = np.array(highlight).reshape(-1)\n    for i in highlight:\n        self.variableNames[i] = highlighter + self.variableNames[i] + ender\nstring = '$f(x, '\nstring += ', '.join([x for x in self.variableNames]) + ') = '\nstring += '\\\\left\\\\{ \\\\stackrel{' + str(self.variableNames[0]) + 'x+' + str(\n    self.variableNames[1]) + ' \\\\quad x<\\\\frac{' + str(self.variableNames[2]\n    ) + '-' + str(self.variableNames[1]) + '}{' + str(self.variableNames[0]\n    ) + '}}{ ' + str(self.variableNames[2]) + ' \\\\quad x\\\\geq\\\\frac{' + str(\n    self.variableNames[2]) + '-' + str(self.variableNames[1]) + '}{' + str(self\n    .variableNames[0]) + '}} \\\\right. $'\nif not highlight is None:\n    for i in highlight:\n        self.variableNames[i] = self.variableNames[i][len(highlighter):-len\n            (ender)]\nreturn string\n"
}